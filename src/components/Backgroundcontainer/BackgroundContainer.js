const BackgroundContainer = ({children}) => {
    return (
        <>
            <div className="bgrContainer">
                {children}
            </div>
        </>
    )
}

export default BackgroundContainer;